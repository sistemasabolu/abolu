<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data noupdate="0">    
    <template id="report_sale_report_saleorder_document_inherit_abolu" inherit_id="sale.report_saleorder_document">
      <xpath expr="//table/thead/tr" position="attributes">
	<attribute name="t-if">not doc.is_from_new_customer</attribute>
      </xpath>
      <xpath expr="//table/thead/tr[1]" position="after">
	<tr t-if="doc.is_from_new_customer">
	  <th class="text-left">Description</th>
	  <th class="text-right">Quantity</th>
	  <!-- <th class="text-right">Unit Price</th> -->
	  <!-- <th t-if="display_discount" class="text-right" groups="sale.group_discount_per_so_line"> -->
	  <!--   <span>Disc.(%)</span> -->
	  <!-- </th> -->
	  <!-- <th class="text-right">Taxes</th> -->
	  <!-- <th class="text-right"> -->
	  <!--   <t groups="account.group_show_line_subtotals_tax_excluded">Amount</t> -->
	  <!--   <t groups="account.group_show_line_subtotals_tax_included">Total Price</t> -->
	  <!-- </th> -->
	</tr>
      </xpath>

      <xpath expr="//table/tbody/t[@t-as='line'][1]" position="attributes">
	<attribute name="t-if">not doc.is_from_new_customer</attribute>
      </xpath>
      <xpath expr="//table/tbody/t[@t-as='line']" position="after">
	<t t-if="doc.is_from_new_customer" t-foreach="doc.order_line" t-as="line">

	  <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
	  <t t-set="current_subtotal" t-value="current_subtotal + line.price_total" groups="account.group_show_line_subtotals_tax_included"/>

	  <tr t-att-class="'bg-200 font-weight-bold o_line_section' if line.display_type == 'line_section' else 'font-italic o_line_note' if line.display_type == 'line_note' else ''">
	    <t t-if="not line.display_type">
	      <td><span t-field="line.name"/></td>
	      <td class="text-right">
		<span t-field="line.product_uom_qty"/>
		<span t-field="line.product_uom" groups="uom.group_uom"/>
	      </td>
	      
	    <!--   <td class="text-right"> -->
	    <!-- 	<span t-field="line.price_unit"/> -->
	    <!--   </td> -->
	    <!--   <td t-if="display_discount" class="text-right" groups="sale.group_discount_per_so_line"> -->
	    <!-- 	<span t-field="line.discount"/> -->
	    <!--   </td> -->
	    <!--   <td class="text-right"> -->
	    <!-- 	<span t-esc="', '.join(map(lambda x: (x.description or x.name), line.tax_id))"/> -->
	    <!--   </td> -->
	    <!--   <td class="text-right o_price_total"> -->
	    <!-- 	<span t-field="line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/> -->
	    <!-- 	<span t-field="line.price_total" groups="account.group_show_line_subtotals_tax_included"/> -->
	    <!--   </td> -->
	    </t>
	    <t t-if="line.display_type == 'line_section'">
	      <td colspan="99">
		<span t-field="line.name"/>
	      </td>
	      <t t-set="current_section" t-value="line"/>
	      <t t-set="current_subtotal" t-value="0"/>
	    </t>
	    <t t-if="line.display_type == 'line_note'">
	      <td colspan="99">
		<span t-field="line.name"/>
	      </td>
	    </t>
	  </tr>

	  <!-- <t t-if="current_section and (line_last or doc.order_line[line_index+1].display_type == 'line_section')"> -->
	  <!--   <tr class="is-subtotal text-right"> -->
	  <!--     <td colspan="99"> -->
	  <!-- 	<strong class="mr16">Subtotal</strong> -->
	  <!-- 	<span t-esc="current_subtotal" t-options="{&quot;widget&quot;: &quot;monetary&quot;, &quot;display_currency&quot;: doc.pricelist_id.currency_id}"/> -->
	  <!--     </td> -->
	  <!--   </tr> -->
	  <!-- </t> -->
	</t>
	
      </xpath>

      <xpath expr="//div[@id='total']" position="attributes">
	<attribute name="t-if">not doc.is_from_new_customer</attribute>
      </xpath>
      
    </template>
  </data>
</odoo>
